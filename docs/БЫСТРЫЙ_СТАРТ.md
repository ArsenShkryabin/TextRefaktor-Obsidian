# 🚀 Быстрый старт

## Установка

1. **Установите зависимости:**
   ```powershell
   npm install
   ```

2. **Соберите плагин:**
   ```powershell
   npm run build
   ```
   
   Это автоматически:
   - ✅ Скомпилирует TypeScript
   - ✅ Соберет main.js
   - ✅ Скопирует файлы в `plugin-files\`

3. **Скопируйте файлы в Obsidian:**
   - Откройте папку `plugin-files\`
   - Скопируйте все 3 файла в `.obsidian\plugins\text-enhancer\`

4. **Включите плагин:**
   - Настройки → Сторонние плагины → Text Enhancer → Включить

5. **Настройте:**
   - Включите тестовый режим для проверки
   - Или введите API ключ для реальной работы

## Использование

### Тестовый режим (без API ключа)

1. Включите "Режим тестирования" в настройках
2. Выделите текст (или оставьте без выделения)
3. Используйте:
   - Иконку ⚡ в sidebar
   - Палитру команд (`Ctrl+P`)
   - Горячие клавиши

### Реальный режим (с API ключом)

1. Введите API ключ в настройках
2. Нажмите "Тестировать" для проверки
3. Используйте плагин как обычно

## Новые функции

- **Режим сравнения**: В модальном окне переключитесь на вкладку "Сравнение"
- **Отмена**: Кнопка "Отменить" возвращает оригинальный текст
- **Горячие клавиши**: `Ctrl+Enter` - применить, `Esc` - отмена
- **Пресеты**: Выберите стиль в настройках
- **Кастомные промпты**: Включите и настройте свои промпты

## Разработка

Для разработки с автоматической пересборкой:
```powershell
npm run dev
```

Это будет автоматически пересобирать плагин при изменении файлов.

