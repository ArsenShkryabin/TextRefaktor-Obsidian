# 🔍 Диагностика ошибок загрузки плагина

## Если плагин не загружается

### 1. Проверьте консоль Obsidian

1. Откройте консоль разработчика:
   - `Ctrl+Shift+I` (Windows/Linux)
   - `Cmd+Option+I` (Mac)
   - Или: Настройки → Расширенные инструменты → Открыть консоль разработчика

2. Перейдите на вкладку **Console**

3. Найдите ошибки (красным цветом)

4. Скопируйте текст ошибки

### 2. Типичные ошибки и решения

#### Ошибка: "Cannot find module"
**Причина:** Не все файлы скопированы или неправильная структура

**Решение:**
1. Убедитесь, что скопированы ВСЕ файлы:
   - `main.js`
   - `manifest.json`
   - `styles.css`

2. Проверьте структуру папки:
   ```
   .obsidian/plugins/text-enhancer/
   ├── main.js
   ├── manifest.json
   └── styles.css
   ```

#### Ошибка: "Plugin failed to load"
**Причина:** Ошибка в коде плагина

**Решение:**
1. Пересоберите плагин:
   ```powershell
   npm run build
   ```

2. Убедитесь, что нет ошибок компиляции

3. Скопируйте новый `main.js` в папку плагина

4. Перезапустите Obsidian

#### Ошибка: "View type already registered"
**Причина:** Плагин пытается зарегистрировать view дважды

**Решение:**
1. Полностью перезапустите Obsidian
2. Отключите и включите плагин заново

### 3. Пошаговая диагностика

#### Шаг 1: Проверка файлов
- [ ] `main.js` существует и не пустой
- [ ] `manifest.json` существует и валидный JSON
- [ ] `styles.css` существует

#### Шаг 2: Проверка сборки
```powershell
npm run build
```
- [ ] Нет ошибок компиляции
- [ ] `main.js` обновлен (проверьте дату изменения)

#### Шаг 3: Проверка копирования
- [ ] Файлы скопированы в `.obsidian/plugins/text-enhancer/`
- [ ] Все 3 файла на месте
- [ ] Файлы не повреждены

#### Шаг 4: Перезапуск
- [ ] Полностью закрыли Obsidian
- [ ] Открыли заново
- [ ] Отключили и включили плагин

### 4. Проверка manifest.json

Убедитесь, что `manifest.json` содержит:
```json
{
  "id": "text-enhancer",
  "name": "Text Enhancer",
  "version": "1.0.0",
  "minAppVersion": "0.15.0",
  "description": "...",
  "author": "ArsenShkryabin",
  "isDesktopOnly": false
}
```

### 5. Проверка версии Obsidian

Плагин требует Obsidian версии **0.15.0** или выше.

Проверьте версию:
- Настройки → О программе → Версия

### 6. Если ничего не помогает

1. **Удалите плагин полностью:**
   - Удалите папку `.obsidian/plugins/text-enhancer/`
   - Отключите плагин в настройках

2. **Пересоберите с нуля:**
   ```powershell
   npm run build
   ```

3. **Установите заново:**
   - Скопируйте файлы из `plugin-files/`
   - Включите плагин

4. **Проверьте консоль на ошибки**

### 7. Отправка отчета об ошибке

Если проблема не решена, соберите информацию:

1. Версия Obsidian
2. Версия плагина (из manifest.json)
3. Текст ошибки из консоли
4. Скриншот ошибки (если есть)
5. Описание шагов для воспроизведения

Создайте issue на GitHub с этой информацией.

