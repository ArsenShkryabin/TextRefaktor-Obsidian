# 🔧 Настройка Git для проекта

## Первоначальная настройка

### 1. Инициализация репозитория (если еще не сделано)

```bash
git init
```

### 2. Добавление всех файлов

```bash
git add .
```

### 3. Первый коммит

```bash
git commit -m "Initial commit: Text Enhancer plugin"
```

### 4. Добавление удаленного репозитория

```bash
# Замените yourusername на ваш GitHub username
git remote add origin https://github.com/yourusername/text-enhancer.git
```

### 5. Отправка в GitHub

```bash
git branch -M main
git push -u origin main
```

## Что включено в репозиторий

✅ **Включено:**
- Исходный код (`src/`, `main.ts`)
- Конфигурация (`package.json`, `tsconfig.json`, `esbuild.config.mjs`)
- Документация (`docs/`, `README.md`)
- Скрипты (`scripts/`)
- Стили (`styles.css`)
- Метаданные (`manifest.json`)
- Лицензия (`LICENSE`)
- Конфигурация Git (`.gitignore`, `.gitattributes`, `.editorconfig`)

❌ **Исключено:**
- `node_modules/` - зависимости (устанавливаются через `npm install`)
- `main.js` - скомпилированный файл (генерируется при сборке)
- `plugin-files/` - готовые файлы для установки (генерируются при сборке)
- `.obsidian/` - настройки Obsidian
- Временные файлы

## Обновление репозитория

### Обычный рабочий процесс

```bash
# 1. Проверьте статус
git status

# 2. Добавьте изменения
git add .

# 3. Создайте коммит
git commit -m "Описание изменений"

# 4. Отправьте в GitHub
git push
```

### Перед отправкой

1. **Убедитесь, что код компилируется:**
   ```bash
   npm run build
   ```

2. **Проверьте, что нет ошибок:**
   ```bash
   npm run build
   ```

3. **Проверьте статус Git:**
   ```bash
   git status
   ```

## Ветки

### Создание ветки для новой функции

```bash
git checkout -b feature/название-функции
```

### Переключение между ветками

```bash
git checkout main          # Переключиться на main
git checkout feature/...    # Переключиться на feature ветку
```

### Слияние ветки

```bash
git checkout main
git merge feature/название-функции
```

## Теги для релизов

### Создание тега

```bash
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

### Просмотр тегов

```bash
git tag
```

## Полезные команды

```bash
# Просмотр истории
git log --oneline

# Просмотр изменений
git diff

# Отмена изменений в файле
git checkout -- filename

# Отмена последнего коммита (но сохранить изменения)
git reset --soft HEAD~1

# Просмотр удаленных репозиториев
git remote -v
```

## GitHub Actions

Проект настроен с GitHub Actions для:
- Автоматической сборки при push
- Создания релизов

См. `.github/workflows/` для деталей.

## Проблемы?

### Если забыли добавить файл в .gitignore

```bash
# Удалить файл из индекса, но оставить локально
git rm --cached filename

# Затем добавьте в .gitignore и закоммитьте
```

### Если нужно обновить .gitignore

```bash
# Удалить все файлы из индекса
git rm -r --cached .

# Добавить все заново
git add .

# Закоммитить
git commit -m "Update .gitignore"
```

## Структура для Git

```
text-enhancer/
├── .git/              # Git репозиторий (не коммитится)
├── .gitignore         # Игнорируемые файлы
├── .gitattributes     # Атрибуты файлов
├── .editorconfig     # Настройки редактора
├── .github/          # GitHub Actions
├── src/              # Исходный код ✅
├── docs/             # Документация ✅
├── scripts/          # Скрипты ✅
├── main.ts           # Главный файл ✅
├── package.json      # Зависимости ✅
├── tsconfig.json     # TypeScript ✅
├── manifest.json     # Метаданные ✅
├── styles.css        # Стили ✅
├── README.md         # Документация ✅
├── LICENSE           # Лицензия ✅
└── CONTRIBUTING.md   # Руководство по вкладу ✅
```

## Готово!

Теперь ваш проект готов к работе с Git! 🎉

