# Changelog v1.0.2

## 🆕 Новые функции

### Параллельная работа с двумя API провайдерами (Fallback)
- **Резервный провайдер**: Добавлена возможность настройки резервного API провайдера
- **Автоматическое переключение**: Если основной провайдер не отвечает в течение таймаута (по умолчанию 2 минуты), автоматически переключается на резервный
- **Параллельные запросы**: Основной и резервный провайдеры работают параллельно для максимальной скорости
- **Отдельная модель для fallback**: Возможность указать отдельную модель для резервного провайдера
- **Тестирование обоих API**: Кнопка "Тест API" теперь тестирует оба провайдера одновременно

## 🔧 Улучшения

### Настройки
- Добавлен раздел "Резервный провайдер (Fallback)" в настройках
- Настройки для резервного провайдера:
  - Включение/выключение fallback
  - Выбор резервного провайдера (OpenAI, Ollama, Custom API)
  - URL резервного API (для Custom/Ollama)
  - API ключ резервного провайдера
  - **Модель резервного провайдера** (новая функция)
  - Таймаут переключения (настраиваемый, по умолчанию 2 минуты)

### Исправления ошибок
- **Исправлена ошибка 404**: Исправлена нормализация URL, которая вызывала ошибку `/v1/completions/chat/completions`
- Улучшена обработка URL для предотвращения двойных путей
- Исправлены ошибки TypeScript при компиляции

### Тестирование API
- Кнопка "Тест API" теперь показывает результаты для обоих провайдеров
- Детальные сообщения об ошибках для каждого провайдера
- Отображение названий провайдеров в результатах теста

## 📝 Изменения в файлах

### Обновленные файлы
- `src/types.ts` - Добавлены настройки для fallback провайдера и модели
- `src/api/AIService.ts` - Реализован механизм параллельных запросов с fallback, исправлена нормализация URL
- `src/ui/ChatPanel.ts` - Добавлена поддержка fallback в streaming запросах, использование fallback модели
- `src/settings/SettingsTab.ts` - Добавлен UI для настройки fallback провайдера и модели

## 🐛 Исправления

- Исправлена ошибка 404 при использовании Ollama fallback (`/v1/completions/chat/completions` → `/v1/chat/completions`)
- Исправлена нормализация URL для предотвращения двойных путей
- Исправлены ошибки TypeScript при компиляции
- Улучшена обработка ошибок при тестировании API

## 📋 Технические детали

### Механизм Fallback
- Основной запрос запускается немедленно
- Резервный запрос запускается через таймаут (если основной не ответил)
- Используется `Promise.race` для получения первого успешного ответа
- Если основной запрос падает с ошибкой, автоматически пробуется резервный

### Нормализация URL
- Улучшена логика нормализации URL
- Проверка на существование `/chat/completions` перед добавлением
- Замена `/completions` на `/chat/completions` если нужно
- Предотвращение двойных путей

### Модели
- Основной провайдер использует модель из поля "Модель"
- Резервный провайдер использует модель из поля "Модель резервного провайдера" (если указана)
- Если модель резервного провайдера не указана, используется модель основного провайдера

## 🔄 Миграция с v1.0.1

Для пользователей v1.0.1:
- Обновите плагин до версии 1.0.2
- Новые настройки fallback опциональны - плагин работает как раньше, если fallback не включен
- Если хотите использовать fallback, настройте резервный провайдер в настройках

## 📚 Документация

Обновлена документация:
- `README.md` - Добавлена информация о параллельной работе с двумя API
- Описания настроек обновлены с учетом новых функций

## ⚠️ Известные проблемы

- Нет известных проблем

## 🎯 Следующие версии

Планируется:
- Улучшения производительности
- Дополнительные провайдеры API
- Расширенные настройки fallback

