# 📤 Инструкция по выгрузке на GitHub

## ⚠️ Проблема с PowerShell

Из-за кириллических символов в пути (`проект_расширение_обсидиан`) PowerShell может не работать корректно.

## ✅ Решение: Используйте батник

Я создал файл `git-push.bat` - просто запустите его двойным кликом!

## Или выполните команды вручную:

Откройте **Git Bash** или **CMD** (не PowerShell) и выполните:

```bash
# 1. Инициализация
git init

# 2. Добавление remote
git remote add origin https://github.com/ArsenShkryabin/TextRefaktor-Obsidian.git

# 3. Добавление файлов
git add .

# 4. Коммит
git commit -m "Initial commit: Text Enhancer plugin v1.0.0 with all features"

# 5. Установка ветки
git branch -M main

# 6. Отправка
git push -u origin main
```

## Если репозиторий на GitHub не пустой:

Если там уже есть файлы (например, README.md), сначала выполните:

```bash
git pull origin main --allow-unrelated-histories
```

Затем разрешите конфликты (если есть) и выполните:

```bash
git add .
git commit -m "Merge with remote repository"
git push -u origin main
```

## Проверка

После успешной выгрузки проверьте:
- https://github.com/ArsenShkryabin/TextRefaktor-Obsidian
- Все файлы должны быть видны
- README.md должен отображаться

